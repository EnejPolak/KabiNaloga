{
  "name": "kabi-naloga",
  "private": true,
  "scripts": {
    "php": "php -S localhost:8000",
    "sync": "browser-sync start --proxy localhost:8000 --files '**/*.php' '**/*.css' '**/*.js' --port 3000"
  },
  "devDependencies": {
    "browser-sync": "^3.0.0"
  }
}
